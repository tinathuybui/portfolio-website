This a web scraper written in Python. It uses the `BeautifulSoup` library and the result is exported into an excel file using `pandas`.

# Setup

```shell
pip install beautifulsoup4
pip install pandas
python main.py
```
